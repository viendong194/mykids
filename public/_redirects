# Cloudflare Pages SPA routing — chỉ cần 1 rule duy nhất.
# Static files (SVG, JS, CSS, JSON...) luôn được serve trực tiếp bởi Cloudflare Pages.
# Chỉ khi file KHÔNG tồn tại mới fallback về index.html.
/* /index.html 200
