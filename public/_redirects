# Cloudflare Pages Redirects configuration for Single Page Applications (SPA)
# Prevent static assets from being redirected to index.html (which causes XML parser crashes)
/assets/* /assets/:splat 200
/content/* /content/:splat 200

# Redirect all other paths to index.html for SPA routing
/* /index.html 200
